<%= form_for [:admin, @league] do |f| %>
	<div class="form-control">
		<%= f.label :name %>
		<%= f.number_field(:name) %>
	</div>
	<div class="form-control">
		<%= f.label :city %>
		<%= f.text_field(:city) %>
	</div>
		<%= f.submit 'Сохранить', :class => "btn btn-primary m-t-2" %>
<% end %>
